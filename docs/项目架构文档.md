# Claude Relay Service - 架构文档

## 📋 项目概述

Claude Relay Service 是一个**多平台 AI API 中转服务**，作为客户端（Claude Code、Gemini CLI、Codex、Droid CLI 等）与 AI API 之间的中间件，提供认证、限流、监控、定价计算、成本统计等功能。

### 核心价值
- 🔐 **数据安全**: 所有请求仅经过自建服务器，直连各平台 API
- ⚡ **性能可控**: 智能调度、连接池、缓存优化
- 💰 **成本透明**: 详细的 Token 使用和费用统计
- 📊 **监控完整**: 使用情况、成本分析、性能监控
- 🌐 **多平台**: 支持 8 种不同的 AI 服务账户类型

---

## 🏗️ 系统架构概览

```mermaid
graph TB
    subgraph "客户端层 Clients"
        CC[Claude Code]
        GC[Gemini CLI]
        CX[Codex]
        DR[Droid CLI]
        CH[Cherry Studio]
        OT[其他客户端]
    end

    subgraph "反向代理层 Reverse Proxy"
        NG["Nginx / Caddy"]
    end

    subgraph "应用层 Application Layer"
        subgraph "路由层 Routes"
            AR["✓ /api - Claude"]
            GR["✓ /gemini - Gemini"]
            OR["✓ /openai - Codex"]
            DR_R["✓ /droid - Droid"]
            AZ["✓ /azure - Azure"]
            UN["✓ /unified - 统一路由"]
        end

        subgraph "中间件层 Middleware"
            AUTH[API Key 认证]
            RATE[速率限制]
            CONC[并发控制]
            LOG[请求日志]
        end

        subgraph "服务层 Services"
            SCH[统一调度器]
            ACC[账户管理]
            REL[转发服务]
            USG[使用统计]
            PRC[定价计算]
            WHK[Webhook通知]
        end
    end

    subgraph "数据层 Data Layer"
        RD[(Redis)]
    end

    subgraph "外部 API External APIs"
        ANT[Anthropic API]
        GEM[Google Gemini]
        OAI[OpenAI]
        AWS[AWS Bedrock]
        AZU[Azure OpenAI]
        FCT[Factory.ai]
    end

    CC & GC & CX & DR & CH & OT --> NG
    NG --> AR & GR & OR & DR_R & AZ & UN
    AR & GR & OR & DR_R & AZ & UN --> AUTH
    AUTH --> RATE --> CONC --> LOG
    LOG --> SCH
    SCH --> ACC --> REL
    REL --> USG --> PRC --> WHK

    ACC & USG & PRC --> RD
    REL --> ANT & GEM & OAI & AWS & AZU & FCT

    style CC fill:#4A90E2,stroke:#2E5C8A,color:#fff
    style GC fill:#EA4335,stroke:#B71C1C,color:#fff
    style CX fill:#10A37F,stroke:#0D8C6D,color:#fff
    style DR fill:#9C27B0,stroke:#6A1B9A,color:#fff
    style SCH fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style ACC fill:#51CF66,stroke:#2F9E44,color:#fff
    style REL fill:#FFA94D,stroke:#E67700,color:#fff
    style RD fill:#FA5252,stroke:#E03131,color:#fff
```

---

## 🎯 核心技术栈

```mermaid
graph LR
    subgraph "后端技术栈 Backend Stack"
        subgraph "运行环境"
            NODE[Node.js 18+]
        end

        subgraph "Web框架"
            EXP[Express.js]
            CORS[CORS]
            HLM[Helmet]
            CMP[Compression]
        end

        subgraph "数据存储"
            RED[Redis ioredis]
        end

        subgraph "认证 & 安全"
            JWT[JWT]
            BCR[bcryptjs]
            LDAP[ldapjs]
        end

        subgraph "网络 & 代理"
            AXS[Axios]
            HP[https-proxy-agent]
            SP[socks-proxy-agent]
        end

        subgraph "工具库"
            WST[Winston 日志]
            CRN[node-cron 定时]
            GGL[google-auth-library]
            AWS_SDK[AWS SDK]
        end
    end

    subgraph "前端技术栈 Frontend Stack"
        subgraph "核心框架"
            VUE[Vue 3]
            VITE[Vite]
        end

        subgraph "UI & 样式"
            ELP[Element Plus]
            TW[Tailwind CSS]
            FA[FontAwesome]
        end

        subgraph "状态 & 路由"
            PIN[Pinia]
            VR[Vue Router]
        end

        subgraph "工具库"
            AX[Axios]
            CJ[Chart.js]
            DJ[Day.js]
            XL[xlsx]
        end
    end

    NODE --> EXP
    EXP --> RED
    EXP --> JWT
    EXP --> AXS

    VUE --> ELP
    VUE --> PIN
    VUE --> VR

    style NODE fill:#68A063,stroke:#3C6E3A,color:#fff
    style VUE fill:#42B883,stroke:#35495E,color:#fff
    style RED fill:#DC382D,stroke:#A41E11,color:#fff
    style EXP fill:#000000,stroke:#333,color:#fff
```

---

## 🔄 请求处理流程

```mermaid
sequenceDiagram
    autonumber
    participant Client as 客户端<br/>(Claude Code)
    participant NG as Nginx<br/>反向代理
    participant Auth as 认证中间件
    participant Scheduler as 统一调度器
    participant Account as 账户服务
    participant Relay as 转发服务
    participant API as 外部API<br/>(Anthropic)
    participant Redis as Redis
    participant Stats as 统计服务

    Client->>NG: HTTP Request<br/>自建API Key
    NG->>Auth: 转发请求

    rect rgb(255, 235, 205)
    Note over Auth: 认证 & 权限验证
    Auth->>Redis: 验证 API Key
    Redis-->>Auth: Key 信息
    Auth->>Auth: 检查速率限制
    Auth->>Auth: 检查并发限制
    Auth->>Auth: 检查权限/客户端
    end

    rect rgb(224, 255, 224)
    Note over Scheduler,Account: 账户选择
    Auth->>Scheduler: 请求处理
    Scheduler->>Redis: 获取粘性会话
    Scheduler->>Account: 选择可用账户
    Account->>Redis: 获取账户信息
    Account->>Account: 检查Token有效性
    alt Token过期
        Account->>API: 刷新Token (OAuth)
        API-->>Account: 新Token
        Account->>Redis: 更新Token
    end
    end

    rect rgb(230, 230, 250)
    Note over Relay,API: API转发
    Account-->>Scheduler: 返回账户凭据
    Scheduler->>Relay: 使用账户转发
    Relay->>API: 真实API请求<br/>(Bearer Token)
    API-->>Relay: SSE流式响应
    Relay-->>Client: 转发响应流
    end

    rect rgb(255, 240, 245)
    Note over Stats: 使用统计
    Relay->>Stats: 解析Usage数据
    Stats->>Redis: 记录Token使用
    Stats->>Stats: 计算成本
    Stats->>Redis: 更新统计数据
    end

    Note over Client,Stats: 完成请求
```

---

## 🌐 多平台账户架构

```mermaid
graph TB
    subgraph "API Key 管理"
        KEY[API Key]
        PERM[权限配置]
        LIMIT["速率 / 并发限制"]
        CLIENT[客户端限制]
        MODEL[模型黑名单]
    end

    subgraph "统一调度系统"
        USC[Claude调度器<br/>unifiedClaudeScheduler]
        USG[Gemini调度器<br/>unifiedGeminiScheduler]
        USO[OpenAI调度器<br/>unifiedOpenAIScheduler]
        USD[Droid调度器<br/>droidScheduler]
    end

    subgraph "账户类型 Account Types"
        subgraph "Claude系列"
            CLA[Claude Official<br/>OAuth 2.0]
            CON[Claude Console<br/>Session Token]
            BED[AWS Bedrock<br/>AWS Credentials]
            CCR_ACC[CCR<br/>CCR Credentials]
        end

        subgraph "Google系列"
            GEM[Gemini<br/>Google OAuth]
            GAPI[Gemini API<br/>API Key]
        end

        subgraph "OpenAI系列"
            OAI[OpenAI Responses<br/>Codex API Key]
            AZR[Azure OpenAI<br/>Azure Key]
        end

        subgraph "其他平台"
            DRD[Droid<br/>Factory.ai Key]
        end
    end

    subgraph "转发服务 Relay Services"
        CR[claudeRelayService]
        CCR_R[claudeConsoleRelayService]
        BR[bedrockRelayService]
        CCRR[ccrRelayService]
        GR[geminiRelayService]
        OR[openaiResponsesRelayService]
        AR[azureOpenaiRelayService]
        DR[droidRelayService]
    end

    KEY --> PERM & LIMIT & CLIENT & MODEL

    PERM --> USC & USG & USO & USD

    USC --> CLA & CON & BED & CCR_ACC
    USG --> GEM & GAPI
    USO --> OAI & AZR
    USD --> DRD

    CLA --> CR
    CON --> CCR_R
    BED --> BR
    CCR_ACC --> CCRR
    GEM --> GR
    GAPI --> GR
    OAI --> OR
    AZR --> AR
    DRD --> DR

    style USC fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style USG fill:#51CF66,stroke:#2F9E44,color:#fff
    style USO fill:#4DABF7,stroke:#1971C2,color:#fff
    style USD fill:#9C27B0,stroke:#6A1B9A,color:#fff
    style KEY fill:#FFD700,stroke:#B8860B,color:#000
```

---

## 🔐 认证与安全架构

```mermaid
graph TB
    subgraph "客户端请求"
        REQ[HTTP Request<br/>Authorization: Bearer cr_xxx]
    end

    subgraph "认证层 Authentication"
        EXTRACT[提取 API Key]
        HASH[SHA-256 Hash]
        VERIFY[验证 Key 有效性]
    end

    subgraph "权限检查 Permission Check"
        PERM_CHK{权限检查}
        CLIENT_CHK{客户端检查}
        MODEL_CHK{模型黑名单}
        RATE_CHK{速率限制}
        CONC_CHK{并发限制}
        QUEUE{并发排队}
    end

    subgraph "Redis数据"
        KEY_DATA[(API Key数据<br/>api_key:id)]
        KEY_HASH[(Hash映射<br/>api_key_hash:hash)]
        RATE_DATA[(速率计数<br/>rate_limit:key:window)]
        CONC_DATA[(并发计数<br/>concurrency:account)]
        QUEUE_DATA[(排队计数<br/>concurrency:queue:key)]
    end

    subgraph "会话管理 Session"
        STICKY[(粘性会话<br/>sticky_session:hash)]
        SESSION_TTL[TTL: 1小时<br/>可续期]
    end

    REQ --> EXTRACT
    EXTRACT --> HASH
    HASH --> KEY_HASH
    KEY_HASH --> VERIFY
    VERIFY --> KEY_DATA

    KEY_DATA --> PERM_CHK
    PERM_CHK -->|检查all/claude/gemini| CLIENT_CHK
    CLIENT_CHK -->|User-Agent匹配| MODEL_CHK
    MODEL_CHK -->|模型是否允许| RATE_CHK

    RATE_CHK --> RATE_DATA
    RATE_DATA -->|未超限| CONC_CHK
    CONC_CHK --> CONC_DATA
    CONC_DATA -->|超限| QUEUE
    QUEUE --> QUEUE_DATA
    QUEUE_DATA -->|等待排队| PROCESS[处理请求]
    CONC_DATA -->|未超限| PROCESS

    PROCESS --> STICKY
    STICKY --> SESSION_TTL

    RATE_CHK -->|超限| REJECT_429[返回 429]
    QUEUE -->|超时| REJECT_429
    CLIENT_CHK -->|不匹配| REJECT_403[返回 403]
    MODEL_CHK -->|禁止| REJECT_403

    style EXTRACT fill:#4ECDC4,stroke:#2B7A78,color:#fff
    style VERIFY fill:#95E1D3,stroke:#38B2AC,color:#000
    style REJECT_429 fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style REJECT_403 fill:#FA5252,stroke:#E03131,color:#fff
    style PROCESS fill:#51CF66,stroke:#2F9E44,color:#fff
```

---

## 📊 数据流与统计架构

```mermaid
graph LR
    subgraph "请求处理 Request Processing"
        REQ[API 请求]
        RESP[SSE 流式响应]
        USAGE[Usage 数据解析]
    end

    subgraph "统计维度 Statistics Dimensions"
        subgraph "Token统计"
            INPUT[Input Tokens]
            OUTPUT[Output Tokens]
            CACHE_C[Cache Create]
            CACHE_R[Cache Read]
        end

        subgraph "成本计算"
            PRICE[定价服务<br/>pricingService]
            COST[成本计算器<br/>costCalculator]
        end

        subgraph "多维度统计"
            KEY_STATS[按 API Key]
            MODEL_STATS[按模型]
            ACCOUNT_STATS[按账户]
            DAILY_STATS[按日期]
            GLOBAL_STATS[全局统计]
        end
    end

    subgraph "Redis存储 Storage"
        USAGE_KEY[(usage:daily:date:key:model)]
        USAGE_ACC[(usage:account:id:date)]
        USAGE_GLB[(usage:global:date)]
        COST_KEY[(api_key_cost:keyId)]
        CACHE_GLB[(global_cache_stats)]
    end

    subgraph "监控与通知"
        MONITOR[缓存监控<br/>cacheMonitor]
        WEBHOOK[Webhook通知]
        METRICS[实时指标<br/>METRICS_WINDOW]
    end

    REQ --> RESP
    RESP --> USAGE

    USAGE --> INPUT & OUTPUT & CACHE_C & CACHE_R

    INPUT & OUTPUT & CACHE_C & CACHE_R --> PRICE
    PRICE --> COST

    COST --> KEY_STATS & MODEL_STATS & ACCOUNT_STATS & DAILY_STATS & GLOBAL_STATS

    KEY_STATS --> USAGE_KEY & COST_KEY
    MODEL_STATS --> USAGE_KEY
    ACCOUNT_STATS --> USAGE_ACC
    DAILY_STATS --> USAGE_KEY
    GLOBAL_STATS --> USAGE_GLB

    CACHE_C & CACHE_R --> CACHE_GLB
    CACHE_GLB --> MONITOR

    COST --> WEBHOOK
    USAGE --> METRICS

    style USAGE fill:#4ECDC4,stroke:#2B7A78,color:#fff
    style PRICE fill:#FFD93D,stroke:#F9B208,color:#000
    style COST fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style MONITOR fill:#51CF66,stroke:#2F9E44,color:#fff
```

---

## 🔄 OAuth 认证流程

```mermaid
sequenceDiagram
    autonumber
    participant Admin as 管理员
    participant Web as Web界面
    participant Server as CRS服务器
    participant Proxy as SOCKS5/HTTP代理
    participant Claude as Claude API<br/>(OAuth Provider)
    participant Redis as Redis

    rect rgb(240, 248, 255)
    Note over Admin,Server: 1. 生成授权URL
    Admin->>Web: 点击"添加账户"
    Web->>Server: POST /generate-auth-url<br/>代理配置
    Server->>Server: 生成 code_verifier<br/>code_challenge (PKCE)
    Server-->>Web: 返回授权URL
    Web->>Admin: 显示授权链接
    end

    rect rgb(255, 250, 240)
    Note over Admin,Claude: 2. 用户授权
    Admin->>Claude: 打开授权URL<br/>（需科学上网）
    Claude->>Admin: 显示登录/授权页面
    Admin->>Claude: 完成登录和授权
    Claude-->>Admin: 返回 Authorization Code
    end

    rect rgb(240, 255, 240)
    Note over Admin,Redis: 3. 交换Token
    Admin->>Web: 粘贴 Auth Code
    Web->>Server: POST /exchange-code<br/>code + code_verifier

    Server->>Proxy: 通过代理连接
    Proxy->>Claude: POST /oauth2/token<br/>PKCE验证
    Claude-->>Proxy: access_token<br/>refresh_token<br/>expires_in
    Proxy-->>Server: 返回Token数据

    Server->>Server: AES加密Token
    Server->>Redis: 存储加密后的<br/>OAuth数据
    Server-->>Web: 账户创建成功
    end

    rect rgb(255, 240, 245)
    Note over Server,Redis: 4. 自动刷新Token
    loop 定时检查 (10秒提前刷新)
        Server->>Redis: 检查Token过期时间
        alt Token即将过期
            Server->>Proxy: 通过代理连接
            Proxy->>Claude: POST /oauth2/token<br/>grant_type=refresh_token
            Claude-->>Proxy: 新 access_token
            Proxy-->>Server: 返回新Token
            Server->>Server: AES加密
            Server->>Redis: 更新Token
        end
    end
    end

    Note over Admin,Redis: OAuth流程完成
```

---

## 🎨 Web 管理界面架构

```mermaid
graph TB
    subgraph "前端架构 Frontend Architecture"
        subgraph "视图层 Views"
            DASH[仪表板<br/>DashboardView]
            ACC[账户管理<br/>AccountsView]
            KEY[API Keys<br/>ApiKeysView]
            STATS[使用统计<br/>ApiStatsView]
            USER[用户管理<br/>UserManagementView]
            SET[系统设置<br/>SettingsView]
        end

        subgraph "组件层 Components"
            OAUTH[OAuth授权<br/>OAuthFlow]
            PROXY[代理配置<br/>ProxyConfig]
            MODAL[模态框组件]
            CHART[图表组件<br/>Chart.js]
            TABLE[表格组件<br/>Element Plus]
        end

        subgraph "状态管理 State"
            AUTH_S[认证状态<br/>authStore]
            THEME_S[主题状态<br/>themeStore]
            ACCOUNT_S[账户状态]
        end

        subgraph "路由 Router"
            VR[Vue Router]
            GUARD[路由守卫]
        end
    end

    subgraph "后端API Endpoints"
        subgraph "管理API"
            ADMIN_AUTH["✓ /admin/login"]
            ADMIN_DASH["✓ /admin/dashboard"]
            ADMIN_ACC["✓ /admin/claude-accounts"]
            ADMIN_KEY["✓ /admin/api-keys"]
        end

        subgraph "数据API"
            STATS_API["✓ /admin/usage-stats"]
            BALANCE["✓ /admin/account-balance"]
            WEBHOOK_API["✓ /admin/webhook/configs"]
        end
    end

    DASH & ACC & KEY & STATS & USER & SET --> OAUTH & PROXY & MODAL & CHART & TABLE

    DASH & ACC & KEY --> AUTH_S & THEME_S & ACCOUNT_S

    VR --> GUARD
    GUARD --> DASH & ACC & KEY & STATS

    OAUTH --> ADMIN_ACC
    ACC --> ADMIN_ACC
    KEY --> ADMIN_KEY
    STATS --> STATS_API

    AUTH_S --> ADMIN_AUTH
    ACCOUNT_S --> BALANCE

    style DASH fill:#4ECDC4,stroke:#2B7A78,color:#fff
    style OAUTH fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style AUTH_S fill:#FFD93D,stroke:#F9B208,color:#000
    style VR fill:#42B883,stroke:#35495E,color:#fff
```

---

## 🚀 并发控制与排队机制

```mermaid
flowchart TD
    Start([请求到达]) --> VerifyKey[验证 API Key]
    VerifyKey --> CheckRate[检查速率限制]

    CheckRate -->|超限| RateLimit[返回 429]
    CheckRate -->|未超限| CheckConcurrency[检查并发限制]

    CheckConcurrency -->|未满| AcquireSlot[占用并发位]
    CheckConcurrency -->|已满| CheckQueue{检查排队开关}

    AcquireSlot --> Process[处理请求]

    CheckQueue -->|disabled| RejectImmediate[立即返回 429]
    CheckQueue -->|enabled| CheckQueueSize{检查队列容量}

    CheckQueueSize -->|已满| QueueFull[返回 429]
    CheckQueueSize -->|未满| EnterQueue[进入排队]

    EnterQueue --> Poll[轮询等待]
    Poll -->|指数退避| TryAcquire{尝试占位}

    TryAcquire -->|成功| Verify[身份验证]
    TryAcquire -->|失败| Continue[继续等待]
    Continue --> Poll

    Verify -->|通过| Process

    Poll -->|超时| Timeout[返回 429]

    Process --> CallAPI[调用上游 API]
    CallAPI --> Release[释放并发位]
    Release --> End([结束])

    RateLimit --> End
    RejectImmediate --> End
    QueueFull --> End
    Timeout --> End

    style Start fill:#4ECDC4,stroke:#2B7A78,color:#fff
    style Process fill:#51CF66,stroke:#2F9E44,color:#fff
    style RateLimit fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style RejectImmediate fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style QueueFull fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style Timeout fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style End fill:#95E1D3,stroke:#38B2AC,color:#000
```

**并发排队机制说明**:

- **配置参数**:
  - `concurrentRequestQueueEnabled`: 启用/禁用排队（默认 false）
  - `concurrentRequestQueueMaxSize`: 最大排队数
  - `concurrentRequestQueueTimeoutMs`: 排队超时时间（默认 10 秒）
  - `concurrentRequestQueueHealthCheckEnabled`: 健康检查开关

- **智能特性**:
  - 指数退避防惊群（200ms ~ 2s，带 ±20% 抖动）
  - UUID token + Socket 引用双重身份验证
  - TTL 自动清理泄漏计数
  - P90 等待时间健康检查

---

## 🔧 服务启动流程

```mermaid
graph TD
    START([启动应用]) --> LOAD_ENV[加载环境变量<br/>.env]
    LOAD_ENV --> LOAD_CONFIG[加载配置<br/>config/config.js]

    LOAD_CONFIG --> CONNECT_REDIS[连接Redis]
    CONNECT_REDIS --> REDIS_OK{连接成功?}
    REDIS_OK -->|否| EXIT_FAIL([启动失败])
    REDIS_OK -->|是| INIT_BALANCE[初始化账户余额服务<br/>registerAllProviders]

    INIT_BALANCE --> INIT_PRICING[初始化定价服务<br/>pricingService]
    INIT_PRICING --> INIT_MODEL[初始化模型服务<br/>modelService]
    INIT_MODEL --> INIT_CACHE[初始化缓存监控<br/>cacheMonitor]

    INIT_CACHE --> INIT_ADMIN[初始化管理员凭据<br/>data/init.json]
    INIT_ADMIN --> CLEANUP_SESSION[清理无效会话]
    CLEANUP_SESSION --> CHECK_COST[检查成本数据]

    CHECK_COST --> NEED_INIT{需要初始化?}
    NEED_INIT -->|是| INIT_COST[初始化所有API Key成本]
    NEED_INIT -->|否| INIT_SESSION_WIN
    INIT_COST --> INIT_SESSION_WIN[初始化Claude会话窗口]

    INIT_SESSION_WIN --> START_CRON[启动定时任务]

    START_CRON --> CRON_TOKEN[Token刷新任务<br/>每分钟]
    START_CRON --> CRON_CLEANUP[并发清理任务<br/>每分钟]
    START_CRON --> CRON_RATE[速率限制清理<br/>每5分钟]
    START_CRON --> CRON_OVERLOAD[过载状态清理<br/>每分钟]
    START_CRON --> CRON_QUEUE[并发队列清理<br/>启动时]

    CRON_TOKEN & CRON_CLEANUP & CRON_RATE & CRON_OVERLOAD & CRON_QUEUE --> SETUP_ROUTES[设置路由]

    SETUP_ROUTES --> ROUTE_API["✓ /api/* - Claude"]
    SETUP_ROUTES --> ROUTE_GEMINI["✓ /gemini/* - Gemini"]
    SETUP_ROUTES --> ROUTE_OPENAI["✓ /openai/* - OpenAI"]
    SETUP_ROUTES --> ROUTE_ADMIN["✓ /admin/* - 管理API"]
    SETUP_ROUTES --> ROUTE_WEB["✓ /web - 管理界面"]

    ROUTE_API & ROUTE_GEMINI & ROUTE_OPENAI & ROUTE_ADMIN & ROUTE_WEB --> START_SERVER[启动HTTP服务器]

    START_SERVER --> LISTEN[监听端口 3000]
    LISTEN --> READY([服务就绪])

    style START fill:#4ECDC4,stroke:#2B7A78,color:#fff
    style CONNECT_REDIS fill:#FFD93D,stroke:#F9B208,color:#000
    style REDIS_OK fill:#FF6B6B,stroke:#C92A2A,color:#fff
    style START_CRON fill:#95E1D3,stroke:#38B2AC,color:#000
    style READY fill:#51CF66,stroke:#2F9E44,color:#fff
    style EXIT_FAIL fill:#FA5252,stroke:#E03131,color:#fff
```

---

## 📁 目录结构

```
claude-relay-service/
├── src/                        # 源代码主目录
│   ├── app.js                  # 应用入口文件
│   ├── routes/                 # 路由层
│   │   ├── api.js              # Claude API 路由
│   │   ├── geminiRoutes.js     # Gemini 路由
│   │   ├── openaiRoutes.js     # OpenAI 路由
│   │   ├── droidRoutes.js      # Droid 路由
│   │   ├── admin/              # 管理路由
│   │   └── ...                 # 其他路由
│   ├── services/               # 服务层（核心业务逻辑）
│   │   ├── unifiedClaudeScheduler.js    # Claude 统一调度器
│   │   ├── unifiedGeminiScheduler.js    # Gemini 统一调度器
│   │   ├── unifiedOpenAIScheduler.js    # OpenAI 统一调度器
│   │   ├── claudeAccountService.js      # Claude 账户管理
│   │   ├── claudeRelayService.js        # Claude 转发服务
│   │   ├── apiKeyService.js             # API Key 服务
│   │   ├── pricingService.js            # 定价服务
│   │   ├── userService.js               # 用户服务
│   │   └── ...                          # 其他服务（30+）
│   ├── middleware/             # 中间件
│   │   ├── auth.js             # 认证中间件
│   │   └── browserFallback.js  # 浏览器回退
│   ├── models/                 # 数据模型
│   │   └── redis.js            # Redis 客户端
│   ├── utils/                  # 工具函数
│   │   ├── logger.js           # Winston 日志
│   │   ├── oauthHelper.js      # OAuth 工具
│   │   ├── proxyHelper.js      # 代理工具
│   │   ├── cacheMonitor.js     # 缓存监控
│   │   └── ...                 # 其他工具
│   └── cli/                    # CLI 工具
│       └── initCosts.js        # 成本初始化
├── web/                        # 前端代码
│   └── admin-spa/              # Vue 3 管理界面
│       ├── src/
│       │   ├── views/          # 页面视图
│       │   ├── components/     # 组件
│       │   ├── stores/         # Pinia 状态
│       │   └── router/         # 路由配置
│       └── package.json
├── config/                     # 配置文件
│   ├── config.js               # 主配置
│   └── pricingSource.js        # 定价数据源
├── scripts/                    # 脚本工具
│   ├── setup.js                # 初始化脚本
│   ├── manage.js               # 服务管理
│   └── ...                     # 其他脚本
├── cli/                        # 命令行工具
│   └── index.js                # CLI 入口
├── logs/                       # 日志目录
├── data/                       # 数据目录
│   └── init.json               # 管理员凭据
├── docs/                       # 文档目录
├── .env                        # 环境变量
├── package.json                # 项目配置
└── docker-compose.yml          # Docker 配置
```

---

## 🔑 Redis 数据结构

```mermaid
erDiagram
    API_KEY ||--o{ API_KEY_USAGE : tracks
    API_KEY ||--o{ API_KEY_COST : calculates
    API_KEY ||--o{ RATE_LIMIT : controls
    API_KEY ||--o{ CONCURRENCY : controls

    ACCOUNT ||--o{ USAGE_ACCOUNT : records
    ACCOUNT ||--o{ CONCURRENCY : limits
    ACCOUNT ||--o{ STICKY_SESSION : binds

    API_KEY {
        string id "唯一ID"
        string name "Key名称"
        string hash "SHA-256哈希"
        json permissions "权限配置"
        json limits "速率/并发限制"
        array allowedClients "客户端限制"
        array modelBlacklist "模型黑名单"
    }

    ACCOUNT {
        string id "账户ID"
        string type "类型(官方/Console/Bedrock等)"
        string status "状态(active/inactive)"
        encrypted claudeAiOauth "加密的OAuth数据"
        json proxyConfig "代理配置"
    }

    API_KEY_USAGE {
        string keyId "API Key ID"
        string date "日期"
        string model "模型名称"
        int inputTokens "输入Token"
        int outputTokens "输出Token"
        int cacheCreateTokens "缓存创建"
        int cacheReadTokens "缓存读取"
    }

    API_KEY_COST {
        string keyId "API Key ID"
        float totalCost "总成本"
        float inputCost "输入成本"
        float outputCost "输出成本"
    }

    RATE_LIMIT {
        string keyId "API Key ID"
        string window "时间窗口"
        int count "请求计数"
        int ttl "过期时间"
    }

    CONCURRENCY {
        string accountId "账户ID"
        array timestamps "时间戳数组(Sorted Set)"
    }

    STICKY_SESSION {
        string sessionHash "会话Hash"
        string accountId "绑定的账户ID"
        int ttl "过期时间(1小时)"
    }

    USAGE_ACCOUNT {
        string accountId "账户ID"
        string date "日期"
        json stats "使用统计"
    }
```

---

## 🎯 核心特性说明

### 1. 统一调度系统
- **智能账户选择**: 根据模型、会话hash、权限自动选择最优账户
- **粘性会话**: 同一会话（session_id）始终使用同一账户，支持自动续期
- **负载均衡**: 跨多个账户类型的智能调度
- **故障转移**: 账户异常时自动切换到其他可用账户

### 2. 多账户类型支持
支持 8 种账户类型：
- **claude-official**: Claude 官方 OAuth
- **claude-console**: Claude Console Session
- **bedrock**: AWS Bedrock
- **ccr**: CCR Credentials
- **droid**: Factory.ai
- **gemini**: Google Gemini OAuth
- **openai-responses**: OpenAI Responses (Codex)
- **azure-openai**: Azure OpenAI

### 3. 并发请求排队
- **工作原理**: "先占后检查"模式，轮询尝试占位
- **指数退避**: 200ms~2s，带±20%抖动防惊群
- **Socket身份验证**: UUID token + socket引用双重验证
- **智能清理**: TTL保护，自动清理泄漏计数
- **健康检查**: P90等待时间过载保护

### 4. 安全与加密
- **API Key哈希**: SHA-256存储
- **数据加密**: AES加密敏感数据（OAuth token、credentials）
- **解密缓存**: LRU缓存优化性能
- **权限控制**: 细粒度权限（all/claude/gemini/openai）
- **客户端限制**: 基于User-Agent的客户端识别

### 5. 成本与统计
- **实时统计**: 捕获真实usage数据（input/output/cache）
- **多维度**: 按Key、模型、账户、日期统计
- **成本计算**: 自动计算token成本
- **缓存监控**: 全局缓存统计和命中率
- **Webhook通知**: 事件通知系统

---

## 🚀 部署架构

```mermaid
graph TB
    subgraph "生产环境 Production"
        subgraph "边缘层 Edge"
            CDN[CDN<br/>静态资源]
            DNS[DNS]
        end

        subgraph "接入层 Ingress"
            LB[负载均衡器]
            NG1[Nginx 1]
            NG2[Nginx 2]
        end

        subgraph "应用层 Application"
            APP1[CRS实例1<br/>Node.js]
            APP2[CRS实例2<br/>Node.js]
            APP3[CRS实例3<br/>Node.js]
        end

        subgraph "数据层 Data"
            REDIS_M[Redis主节点]
            REDIS_S1[Redis从节点1]
            REDIS_S2[Redis从节点2]
        end

        subgraph "监控层 Monitoring"
            PROM[Prometheus]
            GRAF[Grafana]
            ALERT[AlertManager]
        end
    end

    DNS --> CDN
    DNS --> LB
    CDN --> LB
    LB --> NG1 & NG2
    NG1 --> APP1 & APP2
    NG2 --> APP2 & APP3

    APP1 & APP2 & APP3 --> REDIS_M
    REDIS_M --> REDIS_S1 & REDIS_S2

    APP1 & APP2 & APP3 --> PROM
    PROM --> GRAF
    PROM --> ALERT

    style LB fill:#4ECDC4,stroke:#2B7A78,color:#fff
    style REDIS_M fill:#DC382D,stroke:#A41E11,color:#fff
    style PROM fill:#E6522C,stroke:#C13018,color:#fff
```

---

## 📝 总结

Claude Relay Service 是一个功能完整、架构清晰的多平台 AI API 中转服务，具备：

✅ **多平台支持**: 8种账户类型无缝集成
✅ **高性能**: 连接池、缓存、并发控制
✅ **高可用**: 智能调度、故障转移、健康检查
✅ **安全可靠**: 多层加密、权限控制、客户端限制
✅ **易于管理**: 现代化 Web 界面、CLI 工具
✅ **成本透明**: 详细的使用统计和成本计算
✅ **可扩展**: 模块化设计、统一调度系统

---

*文档生成时间: 2026-01-10*
*项目版本: v1.0.0*
